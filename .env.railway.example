# ============================================
# RAILWAY.APP DEPLOYMENT ENVIRONMENT VARIABLES
# ============================================
# Copy these to your Railway project's environment variables
# Railway Dashboard > Your Project > Variables

# ============================================
# CRITICAL DJANGO SETTINGS
# ============================================
# Generate a new SECRET_KEY for production:
# python -c "from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())"
SECRET_KEY=your-production-secret-key-50-chars-minimum

# MUST be False in production!
DEBUG=False

# Railway automatically provides RAILWAY_PUBLIC_DOMAIN
# But you can also manually set ALLOWED_HOSTS (comma-separated)
ALLOWED_HOSTS=yourdomain.com,www.yourdomain.com

# ============================================
# DATABASE CONFIGURATION
# ============================================
# Railway automatically provides DATABASE_URL when you add PostgreSQL
# No need to set these manually - Railway handles it!
# DATABASE_URL=postgresql://user:password@host:5432/dbname

# Optional: Explicit PostgreSQL config (only if not using DATABASE_URL)
# POSTGRES_DB=jersey_events
# POSTGRES_USER=postgres
# POSTGRES_PASSWORD=your_password
# POSTGRES_HOST=containers-us-west-1.railway.app
# POSTGRES_PORT=5432

# ============================================
# EMAIL CONFIGURATION
# ============================================
# Choose your email provider: gmail, sendgrid, mailgun, or console
EMAIL_PROVIDER=sendgrid

# For Gmail (using Google Workspace)
# EMAIL_HOST_USER=noreply@yourdomain.com
# EMAIL_HOST_PASSWORD=your-app-specific-password

# For SendGrid
SENDGRID_API_KEY=your-sendgrid-api-key

# For Mailgun
# MAILGUN_SMTP_USER=postmaster@your-domain.mailgun.org
# MAILGUN_SMTP_PASSWORD=your-mailgun-password

# Default from email
DEFAULT_FROM_EMAIL=Jersey Events <noreply@yourdomain.com>

# ============================================
# SUMUP PAYMENT CONFIGURATION
# ============================================
# ⚠️ CRITICAL: Get these from your SumUp production dashboard
# NEVER use test credentials in production!
SUMUP_API_URL=https://api.sumup.com/v0.1
SUMUP_CLIENT_ID=your_production_client_id
SUMUP_CLIENT_SECRET=your_production_client_secret
SUMUP_MERCHANT_CODE=your_merchant_code
SUMUP_ACCESS_TOKEN=your_access_token
SUMUP_API_KEY=your_api_key
SUMUP_MERCHANT_ID=your_merchant_id
SUMUP_REDIRECT_URI=https://yourdomain.com/payments/sumup/callback/
SUMUP_SUCCESS_URL=/payments/success/
SUMUP_FAIL_URL=/payments/fail/

# Payment provider (sumup, stripe, or citypay)
PAYMENT_PROVIDER=sumup

# ============================================
# SITE CONFIGURATION
# ============================================
# Your production site URL (used in emails and payment callbacks)
SITE_URL=https://yourdomain.com

# Environment name for monitoring
ENVIRONMENT=production

# Release version (optional, for Sentry tracking)
RELEASE_VERSION=1.0.0

# ============================================
# MONITORING & ERROR TRACKING (OPTIONAL BUT RECOMMENDED)
# ============================================
# Sign up at https://sentry.io for free error monitoring
SENTRY_DSN=https://your-sentry-dsn@sentry.io/project-id

# ============================================
# PRICING CONFIGURATION (OPTIONAL - HAS DEFAULTS)
# ============================================
# Platform currency
PLATFORM_CURRENCY=GBP

# Tier-based pricing (defaults shown)
TIER_1_CAPACITY=50
TIER_1_FEE=0.50

TIER_2_CAPACITY=100
TIER_2_FEE=0.45

TIER_3_CAPACITY=250
TIER_3_FEE=0.40

TIER_4_CAPACITY=400
TIER_4_FEE=0.35

TIER_5_CAPACITY=500
TIER_5_FEE=0.30

# Maximum auto-pricing capacity
MAX_AUTO_CAPACITY=500

# Contact email for custom pricing
CUSTOM_PRICING_EMAIL=admin@yourdomain.com

# SumUp processing rate (default 1.69%)
SUMUP_PROCESSING_RATE=0.0169

# ============================================
# NOTES FOR RAILWAY DEPLOYMENT
# ============================================
# 1. DATABASE: Add PostgreSQL plugin in Railway dashboard
#    - Railway will automatically set DATABASE_URL
#    - No manual database configuration needed!
#
# 2. DOMAIN: Railway provides a free subdomain (*.up.railway.app)
#    - Custom domain: Add in Railway dashboard > Settings > Domains
#    - Railway automatically sets RAILWAY_PUBLIC_DOMAIN
#
# 3. BUILD: Railway automatically detects Django and runs:
#    - pip install -r requirements.txt
#    - python manage.py collectstatic --noinput
#    - python manage.py migrate --noinput
#
# 4. DEPLOY: Railway runs the web process from Procfile
#    - gunicorn events.wsgi:application
#
# 5. LOGS: View in Railway dashboard > Deployments > [Latest] > Logs
#
# 6. WORKER (OPTIONAL): For Django-Q background tasks
#    - Add a new service in Railway
#    - Use the same repo
#    - Set command: python manage.py qcluster
#
# 7. ENVIRONMENT VARIABLES: Set in Railway dashboard
#    - Project > Variables tab
#    - Copy values from this file
#    - Click "Add Variable" for each one
#
# 8. SECURITY CHECKLIST:
#    ✅ SECRET_KEY is set to a strong random value
#    ✅ DEBUG=False
#    ✅ ALLOWED_HOSTS includes your domain
#    ✅ SumUp credentials are production credentials
#    ✅ Email provider is configured
#    ✅ Database backups are configured (Railway Pro)
#    ✅ HTTPS is enabled (automatic on Railway)
